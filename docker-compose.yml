services:
  crypton:
    build: .
    image: ghcr.io/aghalla/crypton-deploy:latest
    ports:
      - "8000:8000"
    env_file: .env
    volumes:
      - ./db.sqlite3:/app/db.sqlite3
      - ./ai_models:/app/ai_models
    restart: always
